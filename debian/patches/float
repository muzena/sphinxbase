--- a/test/unit/test_fe/test_pitch.c
+++ b/test/unit/test_fe/test_pitch.c
@@ -14,7 +14,8 @@ main(int argc, char *argv[])
 	yin_t *pe;
 	int16 *buf;
 	size_t nsamp, start;
-	uint16 period, bestdiff;
+	uint16 period;
+	float bestdiff;
 	int nfr;
 
 	/* To make life easier, read the whole thing. */
